import { FormEvent, ReactNode, useEffect, useMemo, useState } from "react";
import { calculateRoofSnow, getClimaticLocation, getClimaticLocations, getClimaticProvinces } from "./api";
import type { CalculationMode, CalculationRequest, CalculationResponse } from "./types";

const modes: { value: CalculationMode; label: string }[] = [
  { value: "UNIFORM_ROOF", label: "Uniform roof" },
  { value: "LOWER_ADJACENT_ROOF", label: "Lower adjacent roof" },
  { value: "ROOF_PROJECTION_OR_PARAPET", label: "Projection / parapet" },
];

type HelpKey = "slope" | "surface" | "is" | "cw" | "cb";
const cbRows = [
  [70,.80,.80,.80],[80,.82,.85,.91],[100,.85,.94,1.11],[120,.88,1.01,1.27],[140,.90,1.07,1.40],[160,.92,1.12,1.51],[180,.93,1.16,1.60],[200,.95,1.19,1.67],[220,.96,1.21,1.73],[240,.96,1.24,1.78],[260,.97,1.25,1.82],[280,.98,1.27,1.85],[300,.98,1.28,1.88],[320,.99,1.29,1.90],[340,.99,1.30,1.92],[360,.99,1.30,1.93],[380,.99,1.30,1.95],[400,.99,1.31,1.95],[420,.99,1.31,1.96],[440,1.00,1.32,1.96],[460,1.00,1.32,1.97],[480,1.00,1.32,1.98],[500,1.00,1.32,1.98],[520,1.00,1.33,1.98],[540,1.00,1.33,1.99],[560,1.00,1.33,1.99],[580,1.00,1.33,1.99],[600,1.00,1.33,1.99],[620,1.00,1.33,2.00],
];

const help: Record<HelpKey, { title: string; reference: string; visual: ReactNode; body: ReactNode }> = {
  slope: {
    title: "Roof slope, α",
    reference: "NBCC 2020 4.1.6.2.(5)–(7)",
    visual: <svg viewBox="0 0 220 90" aria-hidden="true"><path d="M25 70H195M45 70L165 25"/><path d="M76 70A31 31 0 0 1 74 59"/><text x="82" y="61">α</text></svg>,
    body: <><p><b>Standard roof — Cs</b></p><table className="help-table"><tbody><tr><td>α ≤ 30°</td><td>Cs = 1.0</td></tr><tr><td>30° &lt; α ≤ 70°</td><td>Cs = (70° − α) / 40°</td></tr><tr><td>α &gt; 70°</td><td>Cs = 0</td></tr></tbody></table><p><b>Unobstructed slippery roof</b></p><table className="help-table"><tbody><tr><td>α ≤ 15°</td><td>Cs = 1.0</td></tr><tr><td>15° &lt; α ≤ 60°</td><td>Cs = (60° − α) / 45°</td></tr><tr><td>α &gt; 60°</td><td>Cs = 0</td></tr></tbody></table><p className="help-note">When Cs is used with accumulation factors for increased snow loads, NBCC states Cs = 1.0 unless otherwise stated in the Subsection.</p></>,
  },
  surface: {
    title: "Roof surface",
    reference: "NBCC 2020 4.1.6.2.(5)–(7)",
    visual: <svg viewBox="0 0 220 90" aria-hidden="true"><path d="M20 65L90 30M125 65L195 30"/><circle cx="52" cy="43" r="5"/><path d="M154 44l22-11M172 31l-5 9"/><text x="30" y="82">Normal</text><text x="132" y="82">Slippery</text></svg>,
    body: <><p><b>Normal</b> — use the standard roof-slope rules in Sentence (5).</p><p><b>Smooth / slippery</b> — use the special Sentence (6) rule only for an <b>unobstructed slippery roof where snow and ice can slide completely off the roof</b>.</p><p className="help-note">If snow cannot slide completely off because of obstructions or roof configuration, do not assume the slippery-roof reduction.</p></>,
  },
  is: {
    title: "Importance factor, Is",
    reference: "NBCC 2020 Table 4.1.6.2.-A",
    visual: <svg viewBox="0 0 220 90" aria-hidden="true"><path d="M55 72V35L110 15l55 20v37M40 72h140M88 72V48h44v24"/><text x="92" y="43">Is</text></svg>,
    body: <><p>Select the building Importance Category, then use the factor for the applicable limit state.</p><table className="help-table help-table-wide"><thead><tr><th>Importance category</th><th>ULS</th><th>SLS</th></tr></thead><tbody><tr><td>Low</td><td>0.80</td><td>0.90</td></tr><tr><td>Normal</td><td>1.00</td><td>0.90</td></tr><tr><td>High</td><td>1.15</td><td>0.90</td></tr><tr><td>Post-disaster</td><td>1.25</td><td>0.90</td></tr></tbody></table><p className="help-note">The building's Importance Category is established elsewhere in NBCC. Do not choose the category from occupancy name alone if the project classification is uncertain.</p></>,
  },
  cw: {
    title: "Wind exposure factor, Cw",
    reference: "NBCC 2020 4.1.6.2.(3)–(4)",
    visual: <svg viewBox="0 0 220 90" aria-hidden="true"><path d="M25 28h75M15 43h105M35 58h70M135 70V43l28-15 28 15v27M128 70h70"/><text x="56" y="23">wind</text><text x="151" y="61">Cw</text></svg>,
    body: <><div className="help-callout"><b>Default: Cw = 1.0</b><span>Use 1.0 except where the specific reduction in Sentence (4) is permitted.</span></div><p><b>Cw may be reduced to 0.75</b> only for buildings in the <b>Low or Normal Importance Categories</b>, in rural areas only or exposed areas north of the tree line, and only when all of these conditions are satisfied:</p><ul><li>Building is exposed on all sides to wind over open terrain and is expected to remain so during its life.</li><li>The roof area is exposed to wind on all sides with no significant roof obstruction nearby, such as parapet walls, within the Code-prescribed clearance distance.</li><li>The loading does <b>not</b> involve accumulation of snow due to drifting from adjacent surfaces.</li></ul><p className="help-note">For adjacent-surface drift calculations, keep Cw = 1.0.</p></>,
  },
  cb: {
    title: "Basic roof factor, Cb",
    reference: "NBCC 2020 4.1.6.2.(2) and Table 4.1.6.2.-B",
    visual: <svg viewBox="0 0 220 90" aria-hidden="true"><path d="M30 65h160M50 65l55-38 65 38"/><path d="M61 53h98" strokeDasharray="5 4"/><text x="96" y="48">Cb</text></svg>,
    body: <><div className="help-callout"><b>Quick rule</b><span>Cb = 0.80 when lc ≤ 70 / Cw².</span></div><p>For <b>lc &gt; 70 / Cw²</b>, use Table 4.1.6.2.-B. Linear interpolation is permitted for intermediate values of <b>lc Cw²</b> or <b>Cw</b>.</p><div className="help-table-scroll"><table className="help-table help-table-wide"><thead><tr><th>lc Cw²</th><th>Cw = 1.0</th><th>Cw = 0.75</th><th>Cw = 0.50</th></tr></thead><tbody>{cbRows.map(r => <tr key={r[0]}><td>{r[0]}</td><td>{Number(r[1]).toFixed(2)}</td><td>{Number(r[2]).toFixed(2)}</td><td>{Number(r[3]).toFixed(2)}</td></tr>)}</tbody></table></div><p className="help-note">NBCC also states Cb = 1.0 for a roof structure with mean height less than 1 + Ss/γ m above grade. Here lc is the characteristic roof length defined by the Code; γ is the specific weight of snow on roofs.</p></>,
  },
};

function HelpButton({ id, open, setOpen }: { id: HelpKey; open: HelpKey|null; setOpen: (v: HelpKey|null) => void }) {
  const item = help[id];
  return <span className="help-wrap"><button type="button" className="help-button" aria-label={`Help for ${item.title}`} aria-expanded={open === id} onClick={() => setOpen(open === id ? null : id)}>?</button>{open === id && <span className="help-popover" role="dialog"><span className="help-title">{item.title}</span><span className="help-reference">{item.reference}</span><span className="help-visual">{item.visual}</span><span className="help-body">{item.body}</span><small>Selection aid based on the cited NBCC 2020 provision. Project-specific engineering judgment and jurisdictional requirements still apply.</small></span>}</span>;
}

function NumberField({ label, value, onChange, unit, readOnly = false, helpId, openHelp, setOpenHelp }: { label: string; value: number; onChange: (n: number) => void; unit?: string; readOnly?: boolean; helpId?: HelpKey; openHelp?: HelpKey|null; setOpenHelp?: (v: HelpKey|null) => void }) {
  return <label className="field"><span className="field-label">{label}{helpId && setOpenHelp && <HelpButton id={helpId} open={openHelp ?? null} setOpen={setOpenHelp}/>}</span><div className="input"><input type="number" step="any" value={value} readOnly={readOnly} onChange={e => onChange(Number(e.target.value))}/><small>{unit}</small></div></label>;
}

function ReferenceFigure({ src, title, reference }: { src: string; title: string; reference: string }) { return <aside className="engineering-figure" aria-label={title}><div className="figure-heading"><div><span>GEOMETRY REFERENCE</span><b>{title}</b></div><small>{reference}</small></div><img src={src} alt={title}/><p>Use this diagram to identify the physical dimensions only. Enter verified project dimensions; do not scale values from the figure.</p></aside>; }

export default function App() {
  const [mode, setMode] = useState<CalculationMode>("UNIFORM_ROOF"); const [ss, setSs] = useState(2.5), [sr, setSr] = useState(0.4);
  const [province, setProvince] = useState(""), [location, setLocation] = useState(""); const [provinces, setProvinces] = useState<string[]>([]), [locations, setLocations] = useState<string[]>([]); const [climaticSource, setClimaticSource] = useState("");
  const [slope, setSlope] = useState(10), [isFactor, setIsFactor] = useState(1), [cw, setCw] = useState(1), [cb, setCb] = useState(0.8); const [surface, setSurface] = useState<"normal"|"smooth_slippery">("normal"); const [openHelp, setOpenHelp] = useState<HelpKey|null>(null);
  const [caseId, setCaseId] = useState<"I"|"II"|"III">("I"); const [ls, setLs] = useState(20), [ws, setWs] = useState(10), [stepHeight, setStepHeight] = useState(2), [parapetHeight, setParapetHeight] = useState(0); const [projectionHeight, setProjectionHeight] = useState(1), [projectionLength, setProjectionLength] = useState(3); const [result, setResult] = useState<CalculationResponse|null>(null), [error, setError] = useState(""), [busy, setBusy] = useState(false);
  useEffect(() => { getClimaticProvinces().then(setProvinces).catch(err => setError(err instanceof Error ? err.message : "Failed to load provinces.")); }, []);
  async function changeProvince(next: string) { setProvince(next); setLocation(""); setLocations([]); setClimaticSource(""); setResult(null); if (!next) return; try { setError(""); setLocations(await getClimaticLocations(next)); } catch (err) { setError(err instanceof Error ? err.message : "Failed to load locations."); } }
  async function changeLocation(next: string) { setLocation(next); setResult(null); if (!province || !next) return; try { setError(""); const data = await getClimaticLocation(province, next); setSs(data.ss); setSr(data.sr); setClimaticSource(data.source); } catch (err) { setError(err instanceof Error ? err.message : "Failed to load climatic data."); } }
  const payload = useMemo<CalculationRequest>(() => { const base: CalculationRequest = { mode, common: { ss, sr_climatic: sr, roof_slope_alpha: slope, roof_surface_type: surface, is: isFactor, cw, cb, adjacent_surface_drift_applicable: mode === "LOWER_ADJACENT_ROOF" }, distribution_points: 10 }; if (mode === "LOWER_ADJACENT_ROOF") base.lower_roof_cases = [{ case_id: caseId, source_surface: "Upper roof", receiving_surface: "Lower roof", drift_direction: "Toward roof step", ls, ws, step_height: stepHeight, parapet_height: parapetHeight, applicability_status: "APPLICABLE", interpretation_note: "Explicit structured case geometry" }]; if (mode === "ROOF_PROJECTION_OR_PARAPET") base.projection = { projection_height: projectionHeight, projection_longest_dimension: projectionLength }; return base; }, [mode, ss, sr, slope, surface, isFactor, cw, cb, caseId, ls, ws, stepHeight, parapetHeight, projectionHeight, projectionLength]);
  async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); setError(""); try { setResult(await calculateRoofSnow(payload)); } catch (err) { setResult(null); setError(err instanceof Error ? err.message : "Calculation failed."); } finally { setBusy(false); } }
  const peak = result?.final_results["peak_snow_load_kpa"] ?? result?.final_results["governing_snow_load_kpa"];
  return <main><header className="hero"><div><p className="eyebrow">LINKOTECH ENGINEERING</p><h1>NBCC 2020 Roof Snow Calculator</h1><p>Validated calculation engine with traceable inputs, engineering geometry and report-ready results.</p></div><span className="badge">NBCC 2020</span></header><div className="layout"><form className="panel" onSubmit={submit}><div className="heading"><div><p className="eyebrow">INPUTS</p><h2>Snow parameters</h2></div></div><label className="field"><span>Roof configuration</span><select value={mode} onChange={e => { setMode(e.target.value as CalculationMode); setResult(null); }}>{modes.map(m => <option key={m.value} value={m.value}>{m.label}</option>)}</select></label><section className="mode"><p><strong>Project location</strong></p><div className="grid"><label className="field"><span>Province / territory</span><select value={province} onChange={e => changeProvince(e.target.value)}><option value="">Select province</option>{provinces.map(p => <option key={p}>{p}</option>)}</select></label><label className="field"><span>Location</span><select value={location} disabled={!province} onChange={e => changeLocation(e.target.value)}><option value="">{province ? "Select location" : "Select province first"}</option>{locations.map(l => <option key={l}>{l}</option>)}</select></label></div>{climaticSource && <p className="note">Climatic values loaded automatically from {climaticSource}.</p>}</section><div className="grid"><NumberField label="Ground snow load, Ss" unit="kPa" value={ss} onChange={setSs} readOnly={Boolean(location)}/><NumberField label="Associated rain load, Sr" unit="kPa" value={sr} onChange={setSr} readOnly={Boolean(location)}/><NumberField label="Roof slope, α" unit="deg" value={slope} onChange={setSlope} helpId="slope" openHelp={openHelp} setOpenHelp={setOpenHelp}/><label className="field"><span className="field-label">Roof surface<HelpButton id="surface" open={openHelp} setOpen={setOpenHelp}/></span><select value={surface} onChange={e => setSurface(e.target.value as typeof surface)}><option value="normal">Normal</option><option value="smooth_slippery">Smooth / slippery</option></select></label><NumberField label="Importance factor, Is" value={isFactor} onChange={setIsFactor} helpId="is" openHelp={openHelp} setOpenHelp={setOpenHelp}/><NumberField label="Wind exposure factor, Cw" value={cw} onChange={setCw} helpId="cw" openHelp={openHelp} setOpenHelp={setOpenHelp}/><NumberField label="Basic roof factor, Cb" value={cb} onChange={setCb} helpId="cb" openHelp={openHelp} setOpenHelp={setOpenHelp}/></div>{mode === "LOWER_ADJACENT_ROOF" && <div className="input-figure-layout"><section className="mode geometry-inputs"><p><strong>Source-area geometry</strong></p><p className="note">The figure beside these inputs identifies the corresponding physical dimensions.</p><label className="field"><span>Case</span><select value={caseId} onChange={e => setCaseId(e.target.value as typeof caseId)}><option>I</option><option>II</option><option>III</option></select></label><div className="grid"><NumberField label="Source length, ls" unit="m" value={ls} onChange={setLs}/><NumberField label="Source width, ws" unit="m" value={ws} onChange={setWs}/><NumberField label="Roof step height, h" unit="m" value={stepHeight} onChange={setStepHeight}/><NumberField label="Parapet height, hp" unit="m" value={parapetHeight} onChange={setParapetHeight}/></div>{cw !== 1 && <div className="error">Adjacent-surface drift requires Cw = 1.0.</div>}</section><ReferenceFigure src="/figures/lower-adjacent-roof.svg" title="Lower adjacent roof / drift geometry" reference="Workbook Figure G-5"/></div>}{mode === "ROOF_PROJECTION_OR_PARAPET" && <div className="input-figure-layout"><section className="mode geometry-inputs"><p><strong>Projection / parapet geometry</strong></p><p className="note">The figure beside these inputs identifies h, l₀, x and the drift region.</p><div className="grid"><NumberField label="Projection height, h" unit="m" value={projectionHeight} onChange={setProjectionHeight}/><NumberField label="Longest dimension, l0" unit="m" value={projectionLength} onChange={setProjectionLength}/></div></section><ReferenceFigure src="/figures/projection-parapet.svg" title="Roof obstruction / parapet drift geometry" reference="Workbook Figure G-8"/></div>}<button className="primary" disabled={busy}>{busy ? "Calculating…" : "Run calculation"}</button>{error && <div className="error">{error}</div>}</form><section className="panel results"><div className="heading"><div><p className="eyebrow">RESULTS</p><h2>Governing calculation</h2></div>{result && <span className="status">{result.calculation_status}</span>}</div>{!result ? <div className="empty"><b>Ready to calculate</b><span>Run the validated NBCC 2020 engine to review results.</span></div> : <><div className="cards"><article><span>Peak / governing snow load</span><b>{typeof peak === "number" ? `${peak.toFixed(3)} kPa` : "—"}</b></article><article><span>Snow density, γ</span><b>{Number(result.derived_parameters["gamma_kn_m3"]).toFixed(3)} kN/m³</b></article><article><span>Slope factor, Cs</span><b>{Number(result.derived_parameters["cs"]).toFixed(3)}</b></article></div>{result.warnings.length > 0 && <div className="warning"><b>Engineering warnings</b>{result.warnings.map(w => <span key={w}>{w}</span>)}</div>}<h3>Load distribution</h3><div className="table"><div className="row head"><span>x (m)</span><span>Ca</span><span>S (kPa)</span></div>{result.distribution_segments.map((p, i) => <div className="row" key={i}><span>{Number(p.x_m).toFixed(2)}</span><span>{Number(p.ca).toFixed(3)}</span><span>{Number(p.snow_load_kpa).toFixed(3)}</span></div>)}</div><details><summary>Engineering trace</summary><pre>{JSON.stringify({ governing_case: result.governing_case, projection_result: result.projection_result, references: result.references, validation_trace: result.validation_trace }, null, 2)}</pre></details><div className="report"><b>Report preview available</b><span>Official PDF requires authenticated server-side report entitlement.</span></div></>}</section></div><footer>Figures clarify variable meaning and geometry only; calculations use explicit project inputs and unrounded engine values.</footer></main>;
}
